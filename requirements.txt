# Python 依赖（生产环境）
flask>=3.0
gunicorn>=22.0

# 选品数据源（可选启用）
pytrends>=4.9.0          # Google Trends
scrapling>=0.1.0          # Google SERP / 网页抓取
requests>=2.31.0          # Comtrade/WITS API

# 飞书同步（当 LARK_DRY_RUN=false 时启用）
# lark-oapi>=1.2.0        # 飞书官方 SDK
